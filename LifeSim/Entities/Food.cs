using System.Drawing;
using System.Numerics;

namespace LifeSim.Entities;

public class Food(Vector2 position) : Entity(position, Color.Crimson)
{
    public override void Update(float deltaTime) { }
}